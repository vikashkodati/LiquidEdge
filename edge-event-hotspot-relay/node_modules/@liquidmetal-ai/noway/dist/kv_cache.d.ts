/**
 * Key-value cache interface required by NoWay.
 */
export interface KvCacheListKey<Key extends string = string> {
    name: Key;
}
export interface KvCacheListResult<Key extends string = string> {
    keys: KvCacheListKey<Key>[];
}
export interface KvCache<Key extends string = string> {
    /**
     * Get a value from the cache as JSON
     */
    get<T = unknown>(key: Key, type: 'json'): Promise<T | null>;
    /**
     * Put a value into the cache with an optional TTL
     */
    put(key: Key, value: string, options?: {
        expirationTtl?: number;
    }): Promise<void>;
    /**
     * Delete a key from the cache
     */
    delete(key: Key): Promise<void>;
    /**
     * List keys in the cache with an optional prefix filter
     */
    list(options?: {
        prefix?: string;
    }): Promise<KvCacheListResult<Key>>;
}
//# sourceMappingURL=kv_cache.d.ts.map